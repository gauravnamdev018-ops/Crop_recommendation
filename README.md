# Crop_recommendation
A Flask-based machine learning system that predicts the most suitable crop using soil nutrients and weather conditions. It uses a trained Random Forest model to provide fast and accurate crop recommendations through an API.
